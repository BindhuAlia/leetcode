class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        vector<int>dummy;
        int n=nums.size();
        for(int i=0;i<n;i++)
        {
            dummy.push_back(nums[i]);
        }
        
        for(int j=0;j<n;j++)
        {
            dummy.push_back(nums[j]);
            
        }
        return dummy;
    }
};
