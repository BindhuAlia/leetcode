class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        ans=0;
        for i in sentences:
            a=len(i.split())
            if(ans<a):
                ans=a
        return ans
            
            
        
